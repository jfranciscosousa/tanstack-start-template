#!/bin/sh

# Database management script
# This script forwards all commands to drizzle-kit for database operations

set -e  # Exit on error

# Load environment variables
ENV_FILE=.env . bin/load-env

# Forward all arguments to drizzle-kit
echo "🗄️  Running drizzle-kit $@..."
npx drizzle-kit "$@"
